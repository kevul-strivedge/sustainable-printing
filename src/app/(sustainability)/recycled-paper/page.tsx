import RecycledPaper from "@/src/components/pages/RecycledPaper";
import { pageMetadata } from "@/src/constants/seoMeta";

export const metadata = pageMetadata("recycled-paper");

export default function RecycledPaperPage() {
  return (
    <RecycledPaper />
  );
}
